# 项目开发笔记

## 你使用了哪些 AI 工具？如何使用的？

使用的是 **Claude Code**。整体工作流程如下：

1. **第一步**：使用 `/init` 命令让 AI 对整个项目进行理解总结，生成 `CLAUDE.md`。
2. **第二步**：新开窗口，将任务文档、接口文档、表结构提供给 AI，让它对任务进行详细拆分，生成多个任务 md 文档。
3. **第三步**：回到主窗口，要求 AI 先对任务 1 文档内容进行开发和测试，没有问题就继续后续的任务。
4. **上下文管理**：上下文达到 60% 时进行手动上下文压缩。

## 遇到了什么问题？如何解决的？

没有大问题。有一个小问题是刚开始的时候响应的格式内容字段没有按照接口文档中的字段进行返回（比如接口文档是orderNo，AI给出为order_id），导致有数据但无法显示。我就要求严格按照接口文档中的字段格式进行返回。

## 如果有更多时间，你还会做什么？

增加一个智能体，让他拥有自动生成日报、周报、月报，库存智能预警和补货建议等功能。还有时间的话，还会把Java部分一并完成。

## 任务 3：Bug 修复

#### 后端

删除前校验是否有关联库存，避免删除后库存数据孤立。 SQLite 默认未开启外键约束，不能依赖数据库外键兜底，必须在应用层校验。

#### 前端

有两个bug。第一个就是修改成功后跳转到第一页，原因是只要调用handleSubmit这个方法就会跳转到第一页，没有做任何的判断。应该为编辑是就地修改，数据条数不变，应停留在当前页，不重置页码，新增商品追加在列表末尾，回到第 1 页便于从首页查看。

第二个是删除后若当前页已空（删掉了最后一页的最后一条）停留在空页。只需判断当前页数是否大于删除后的最大页数，大于就跳转到最大页数。

## 选做 A：出库单 + 库存扣减（已完成）

使用**原子条件 UPDATE** 实现：

```python
stmt = (
    update(Inventory)
    .where(
        Inventory.product_id == item.product_id,
        Inventory.location_code == item.location_code,
        Inventory.quantity >= item.quantity,   # 条件里带库存校验
    )
    .values(quantity=Inventory.quantity - item.quantity, updated_at=datetime.now())
)
if db.execute(stmt).rowcount == 0:
    # 再查一次行是否存在，区分「库存不足」与「库位无库存」两种 400
    ...
```

### 理由

1. 一条 SQL 完成「校验 + 扣减」，条件 `quantity >= n` 不满足时 `rowcount == 0`，天然防超卖，不需要额外字段、不需要重试。
2. SQLite 写事务全局串行：两个并发出库，后执行的那条条件 UPDATE 会因 `quantity` 已被前者扣减而 `rowcount == 0`，从数据库层面杜绝超卖。
3. 实现最贴合任务要求：单事务内逐条扣减，任一失败 `rollback()`，无需引入 `version` 字段或外部依赖。

### 代价

`rowcount == 0` 无法直接区分「库存不足」和「行不存在」，需在扣减后再查一次行是否存在来给出准确的 400 错误信息（这一步不影响并发安全，原子 UPDATE 仍是最终兜底）。

### 后续优化

在微服务集群环境下，可以使用Redis+lua脚本进行预扣减，扣减成功后通过MQ进行异步落库，使用Redisson保证并发安全。

### 前端页面已完成



## 选做 B：单元测试（已完成）

在backend-python的tests目录下

## 选做 C：前端性能优化（已完成）
